name: Extract VBA Macros

on:
  push:
    paths:
      - '*.xlsm'
  workflow_dispatch:
    inputs:
      github_file_url:
        description: 'URL to the Excel file in GitHub'
        required: true
        default: 'https://github.com/koala-man-64/platte-lake-improvement-association/blob/main/03-13-24%20PRW%20PLIA%20Dist%2003-21-24%20153pm.xlsm'
      output_dir:
        description: 'Path to the output directory'
        required: false
        default: 'Extracted Scripts'

jobs:
  extract_vba:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pywin32
          pip install requests

      - name: Run VBA extraction script
        env:
          GITHUB_FILE_URL: ${{ github.event.inputs.github_file_url }}
          OUTPUT_DIR: ${{ github.event.inputs.output_dir }}
        run: |
          python ../.github/workflows/extract_vba.py
